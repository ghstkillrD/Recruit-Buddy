function togglePopup(jobTitle){
  document.getElementById("popup-" + jobTitle).classList.toggle("active");
}

function togglePopupforCourse(courseTitle){
  document.getElementById("popup-" + courseTitle).classList.toggle("active");
}